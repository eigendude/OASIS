{
  "name": "oasis-visualization",
  "scripts": {
    "format": "prettier --write . && eslint . --ext .js --fix",
    "lint": "prettier --check . && eslint . --ext .js"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "prettier": "^3.2.5"
  }
}
